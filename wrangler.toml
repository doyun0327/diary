# Cloudflare Workers — static SPA (Vite dist)
name = "pageby-diary"
compatibility_date = "2025-07-01"
workers_dev = true

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
run_worker_first = false
